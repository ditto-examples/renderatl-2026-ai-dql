export type AlertContextProps = {
    /** Alerts parent node. */
    parentNodeId: string | null;
};
/** Alerts context. */
declare const AlertContext: import("react").Context<AlertContextProps>;
export default AlertContext;
