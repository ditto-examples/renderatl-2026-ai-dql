import React from 'react';
type Props = {
    children: React.ReactNode;
};
/** Alerts provider shares the parent element ID for the alerts to be mounted into. */
declare const AlertProvider: React.FC<Props>;
export default AlertProvider;
